description = "freelance-ops: menu - show all freelance-ops commands or auto-detect from input"
prompt = """
Show the freelance-ops menu or auto-detect and dispatch the user's input to the right mode.
"""
